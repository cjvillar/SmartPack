# SmartPack Camping Forcast: A Streamlit App 

## Start Virtual env:
```bash
python -m venv env

pip install -r requirments.txt
```


## Start Backend API for React/Svelt:
```bash
uvicorn main:app --reload
```

## React Or Svelt:
```bash
const response = await fetch(``);
const data = await response.json();
```


## Start Streamlit App:
```bash
streamlit run app.py
```



